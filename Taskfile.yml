version: '3'

tasks:
  build:
    desc: Compile-check the module
    cmds:
      - go build -o /dev/null ./...

  vet:
    desc: Run go vet
    cmds:
      - go vet ./...

  test:
    desc: Run tests
    cmds:
      - go test ./...
